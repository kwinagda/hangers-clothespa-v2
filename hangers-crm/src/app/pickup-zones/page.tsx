import { PublicContentPage, PublicUnavailable } from '@/components/public/PublicContentPage'
import { getPublicSiteProfile } from '@/lib/publicSite'
import { buildPublicMetadata } from '@/lib/seo'
export const metadata = buildPublicMetadata({ title: 'Dry Cleaning Pickup in Mulund, Bhandup & Thane | Hangers', description: 'Pickup and delivery information for Hangers Clothes Spa customers across Mulund, Bhandup and Thane.', path: '/pickup-zones' })

export default async function PickupZonesPage(){const profile=await getPublicSiteProfile();if(!profile)return <PublicUnavailable/>;const phone=profile.phone.replace(/\D/g,'');const map=`https://www.google.com/maps?q=${encodeURIComponent(profile.address)}&output=embed`;return <PublicContentPage profile={profile} eyebrow="Pickup zones" title="Where we collect and deliver." intro={`Pickup and delivery are available across ${profile.pickupZones.join(', ')} for eligible orders above Rs. ${profile.pickupMinimumOrder}. The team confirms the collection window before a rider is assigned.`}>
  <section className="dp-split"><div><div className="dp-grid two">{profile.pickupZones.map(zone=><article className="dp-card" key={zone}><h2>{zone}</h2><p>Address coverage and the next available collection time are confirmed when you submit the pickup request.</p></article>)}</div><div style={{marginTop:34}}><h2 className="dp-title">Outside the listed areas</h2><p className="dp-copy">Send your location to the Hangers team. We will confirm whether a collection can be arranged; no unverified distance fee or availability is promised on the website.</p></div></div><div><iframe className="dp-map" src={map} title="Hangers Clothes Spa location" loading="lazy" referrerPolicy="no-referrer-when-downgrade"/><div className="dp-band" style={{marginTop:20}}><h2 style={{margin:'0 0 8px',color:'#023c62',fontSize:18}}>Not sure if you are covered?</h2><p className="dp-copy" style={{fontSize:14.5,marginBottom:16}}>Send your address or society name and we will confirm the next available collection slot.</p><a className="dp-btn secondary" href={`https://wa.me/${phone}`} target="_blank" rel="noreferrer">Ask on WhatsApp</a></div></div></section>
  </PublicContentPage>}
